/**
 * Forms the tab name for the browser from a label
 * @param label workspace label, component name, etc.
 * @returns the formatted tab name
 */
export function formTabName(label: string): string {
  return `${label} - Ownly`
}
